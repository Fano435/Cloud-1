- hosts: servers
  remote_user: root

  tasks:
    - name: Update and upgrade the system
      apt:
        update_cache: true
        upgrade: dist

    - name: Check necessary dependencies
      apt:
        name:
          - ca-certificates
          - curl
        state: present

    - name: Create Docker GPG keyring
      file:
        path: /etc/apt/keyrings
        state: directory
        mode: "0755"

    - name: Add Docker GPG key
      get_url:
        url: https://download.docker.com/linux/debian/gpg
        dest: /etc/apt/keyrings/docker.asc
        mode: "0644"

    - name: Add Docker repository
      apt_repository:
        repo: >-
          deb [arch=amd64 signed-by=/etc/apt/keyrings/docker.asc]
          https://download.docker.com/linux/debian
          {{ ansible_distribution_release }} stable
        state: present
        filename: docker

    - name: Install Docker
      apt:
        name:
          - docker-ce
          - docker-ce-cli
          - containerd.io
          - docker-compose-plugin
        state: latest
        update_cache: true

    - name: Add user to the docker group
      user:
        name: "{{ ansible_user }}"
        groups: docker
        append: true

#   - name: Build images and volumes with Docker Compose
#     command: mkdir -p ../data/wordpress && mkdir -p ../data/mariadb && docker compose -f ./srcs/docker-compose.yml build

#   - name: Launch containers with Docker Compose
#     command: docker compose -f ./srcs/docker-compose.yml up -d

# mapping VM-www